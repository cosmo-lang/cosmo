use "assert"
use "math"

int fn lcm(int x, int y) {
  mut int i = 1
  until i % x == 0 &: i % y == 0
    ++i

  i
}

Assert->eq(lcm(9, 18), 18)
Assert->eq(lcm(8, 5), 40)
Assert->eq(lcm(17, 11), 187)

