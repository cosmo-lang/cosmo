any fn doubled(int[] nums) {
  int idx = 0
  int fn generator() {
    int value = nums[idx]
    idx += 1
    nums[idx] * 2
  }
  generator
}

## 2, 4, 6, 8
every int even in doubled([1, 2, 3, 4]) {
  puts(even)
}