func fn doubled(const int[] nums) {
  int idx = 0
  int? fn generator() {
    const int value = nums[idx]
    idx += 1
    unless value == none {
      value * 2
    }
  }
  generator
}

## 2, 4, 6, 8
every int even in doubled([1, 2, 3, 4]) {
  puts(even)
}
