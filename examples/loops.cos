use * from "assert"

mut int sum = 0
every int n in [1, 2, 3, 4, 5, 6, 7, 8] {
  if n % 2 == 0 next
  sum += n
}

Assert::eq(sum, 16)

mut string text = ""
mut int x = 0
until x == 5
  x += 1

Assert::eq(x, 5)
