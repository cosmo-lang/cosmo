use "assert"

string fn domino(string sequence) {
  mut string result = ""
  mut bool finished = false

  every char ch in sequence {
    if ch == '|' &: !finished
      result += '/'
    else {
      if ch == ' ' |: ch == '/'
        finished = true
      result += ch
    }
  }

  result
}

Assert->eq(domino("||//||"), "////||")
Assert->eq(domino("||| ||||//| |/"), "/// ||||//| |/")
Assert->eq(domino("||||"), "////")
