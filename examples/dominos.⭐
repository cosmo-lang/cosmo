string fn domino(string sequence) {
  string result = ""
  bool finished = false

  for char ch in sequence {
    if !finished & (ch == '/' | ch == ' ') {
      finished = true
      result += ch
    } else {
      result += ch
    }
  }

  result
}

puts(domino("||//||")) ## ////||