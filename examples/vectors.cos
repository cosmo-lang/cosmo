use "assert"

string[] letters = ["a", "b", "c"]
puts(letters[0], letters[1], letters[2]) ## a b c

letters << "d"
Assert->eq(letters[3], "d")

int[] nums = [1,2,3,4,5,6]

bool fn even(int n) { n % 2 == 0 }
int[] evens = nums->filter(even)
Assert->eq(evens, [2,4,6])