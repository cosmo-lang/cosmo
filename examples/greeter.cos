any fn greeter(const string greeting) {
  void fn greet(const string person) {
    puts(greeting + " " + person)
  }
}

const func greet = greeter("hello")
greet("world") ## hello world
greet("bob") ## hello bob