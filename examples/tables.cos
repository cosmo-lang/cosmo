type Person = string->any

Person fn new_person(string name, string address) {
  {
    name -> name,
    address -> address
  }
}

Person bobert = new_person("bobert", "123 bobert st")
puts(bobert::name)
puts(bobert->address)