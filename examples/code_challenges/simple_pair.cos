use "beginner_codes"

int[]? fn simple_pair(int[] vec, int n) {
  every int i in #vec
    every int j in (i + 1)..(#vec - 1)
      if vec[i] * vec[j] == n
        return [ vec[i], vec[j] ]
}

BeginnerCodes::test(665, simple_pair)