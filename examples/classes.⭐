public class Dog {
  public const string name

  public Dog fn construct(string name) {
    $->name = name
  }

  public void fn woof() {
    puts($->name + " says woof!")
  }
}

Dog fido = new Dog("Fido")
puts(fido->name) ## Fido says woof!
fido->name = "Bentley" ## error